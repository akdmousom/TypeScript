/**
 * Today i learned about how can i declear type.
 * We have to create an variable without value
 * Now we have to attach the types in the variables
 */

let person:string; //You have to declear the type like this

person = 'AKD';

console.log(person);

person = 22; //Type 'number' is not assignable to type 'string'.

console.log(person);


